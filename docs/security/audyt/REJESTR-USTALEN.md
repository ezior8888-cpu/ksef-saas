# Rejestr ustaleń — audyt bezpieczeństwa

Jeden wiersz = jedno znalezisko. Plik jest jedynym źródłem prawdy o tym,
co znaleźliśmy. Przebieg audytu i zadania dla Bartosza są w dzienniku:
`docs/security/DZIENNIK-AUDYT.md`.

**Tryb audytu: TYLKO RAPORT.** Kolumna „Propozycja naprawy" to opis, nie
wykonana zmiana. Ani jedna linijka w `app/` i `lib/` nie została ruszona.

## Numeracja

| Prefiks | Kategoria | Dzień |
|---|---|---|
| `SEC-A-nn` | sekrety, granica przeglądarka/serwer | 1 |
| `SEC-B-nn` | izolacja najemców w kodzie (obejścia RLS) | 2 |
| `SEC-C-nn` | baza: RLS, polityki, uprawnienia, funkcje | 3 |
| `SEC-D-nn` | wycieki na zewnątrz (model, poczta, logi, RODO) | 4 |
| `SEC-E-nn` | konfiguracja produkcji | 5 |

## Wagi

| Waga | Znaczenie | Co z tym robimy |
|---|---|---|
| **krytyczna** | dane wychodzą poza najemcę albo poza firmę **już teraz**, bez żadnych warunków | stop audytu, zgłoszenie do Igora tego samego dnia |
| **wysoka** | wyciek możliwy, ale wymaga warunku (znajomość identyfikatora, wyścig, konkretna konfiguracja) | blokuje launch |
| **średnia** | nie wycieka, ale osłabia obronę albo ułatwia rozpoznanie atakującemu | naprawa przed launchem, bez pośpiechu |
| **niska** | higiena, dług, dokumentacja rozjechana z kodem | backlog |

Osobna waga **„do ustalenia"** — dla rzeczy, których nie da się rozstrzygnąć
bez wyniku od Bartosza albo bez decyzji produktowej. Każdy taki wpis musi
mieć w kolumnie „Jak odtworzyć" zapisane, **czego konkretnie brakuje**.

---

## Ustalenia

| ID | Waga | Gdzie | Na czym polega | Jak odtworzyć | Co się może stać | Propozycja naprawy | Status |
|---|---|---|---|---|---|---|---|
| SEC-A-01 | **średnia** | `app/api/portal/exports/generate/route.ts`, `app/api/stripe/webhook/route.ts`, `app/api/email/resend-webhook/route.ts`, `app/api/dev/posthog-test/route.ts` | Blok `catch` odsyła treść wyjątku w ciele odpowiedzi HTTP (`e.message`). | `node scripts/security/inventory-entrypoints.ts`, kolumna `errorToClient` w `01-powierzchnia.json`. Ręcznie: koniec pliku `portal/exports/generate/route.ts`. | Komunikat błędu Postgresa potrafi zawierać nazwę tabeli, fragment zapytania i ścieżkę na serwerze. Atakujący dostaje mapę środka aplikacji za darmo, wysyłając celowo błędne żądanie. | Zwracać klientowi stały komunikat i identyfikator zdarzenia; szczegóły wyłącznie do Sentry. Wzorzec jest już w repo — reszta route-ów tak robi. | otwarte |
| SEC-A-02 | **niska** | `docs/security/owasp-top10-mapping.md:84` | Dokument twierdzi: „Stack traces hidden — `console.error` nie leci do response body". SEC-A-01 pokazuje, że w czterech miejscach leci. | Porównać wiersz 84 dokumentu z wynikiem SEC-A-01. | Nieprawdziwy dokument bezpieczeństwa jest gorszy niż jego brak: następny przegląd pominie ten obszar, bo „już sprawdzone". | Poprawić po zamknięciu SEC-A-01. Do rozważenia: przy każdej kontroli dopisać datę i sposób weryfikacji, nie samą nazwę fazy. | otwarte |
| SEC-C-01 | **do ustalenia** | wszystkie 60 tabel, `supabase/migrations/*.sql` | Żadna tabela nie ma `FORCE ROW LEVEL SECURITY`. Postgres nie stosuje polityk RLS do właściciela tabeli. | `grep -ric "force row level security" supabase/migrations/*.sql` → zero trafień. **Brakuje odpowiedzi na pytanie: jaką rolą łączy się PostgREST** — rozstrzyga to `05-postgrest-i-schematy.sql`, punkt 5.4a. | Jeżeli PostgREST łączy się rolą będącą właścicielem tabel, wszystkie polityki RLS są dekoracją i izolacja między klientami nie istnieje. Jeżeli łączy się `authenticated`, ustalenie schodzi do niskiej wagi (obrona w głąb). | Zależna od wyniku. Przy złym wariancie: `ALTER TABLE ... FORCE ROW LEVEL SECURITY` na wszystkich 60 tabelach — migracja, czyli działka Bartosza. | czeka na Bartosza |
| SEC-C-02 | **do ustalenia** | `00014`, `00033`, `00036`, `00046`, `00037`, `00038`, `00053`, `00058` w `supabase/migrations/` | Pliki zawierają więcej deklaracji `SECURITY DEFINER` niż `SET search_path`. Funkcja DEFINER bez przypiętej ścieżki wyszukiwania może wykonać cudzy kod z uprawnieniami właściciela. | Zliczenie wystąpień w plikach migracji. **Stan faktyczny na produkcji sprawdza `04-funkcje-definer.sql`, punkt 4.1** — zliczanie w plikach nie odróżnia funkcji redefiniowanej od nowej. | Podniesienie uprawnień: użytkownik podstawia własną funkcję w schemacie przeszukiwanym wcześniej i wykonuje ją jako właściciel bazy. | `ALTER FUNCTION ... SET search_path = public, pg_temp` dla każdej z listy 4.1. Migracja — działka Bartosza. | czeka na Bartosza |
| SEC-E-01 | **niska** | `docs/security/owasp-top10-mapping.md:35,86` | Dwie kontrole („TLS — Vercel auto-managed", „Disabled directory listing — Vercel default") opierają się na platformie, na której produkcja nie stoi. `AGENTS.md` opisuje Hetzner + Coolify. | Porównać sekcję „Infrastruktura i dostępy" w `AGENTS.md` z wierszami 35 i 86 dokumentu. W repo nadal jest `vercel.json`. | Dwie kontrole bezpieczeństwa uznane za zapewnione przez dostawcę, który ich nie zapewnia, bo go nie używamy. Nikt tego nie sprawdził na Hetznerze. | Zweryfikować obie na produkcji (dzień 5, `audit-headers.ts`) i przepisać wiersze na stan faktyczny. | otwarte |
| SEC-A-03 | **wysoka** | `package.json` — `next: 16.2.6` | Zainstalowany Next.js ma **dziewięć** otwartych doradztw bezpieczeństwa. Wszystkie naprawione w **16.2.11** — podbicie łatki w obrębie tej samej wersji pomniejszej. | `pnpm audit`. Wersja: `node -p "require('./node_modules/next/package.json').version"` → 16.2.6. Zakres podatny: `>=16.0.0 <16.2.11`. | Dwa z dziewięciu trafiają wprost w przedmiot tego audytu. **„Cache confusion of response bodies"** (GHSA-mh99-v99m-4gvg, dwa warianty) — treść odpowiedzi jednego żądania może trafić do innego, czyli wyciek między użytkownikami wynikający z frameworka, nie z naszego kodu. **„Unauthenticated disclosure of internal Server Function endpoints"** (GHSA-mwp4-54f8-5fhr) — ujawnia identyfikatory akcji serwerowych; w połączeniu z ustaleniem dnia 0, że układ strony NIE chroni akcji, to gotowy łańcuch: atakujący poznaje adresy akcji, po czym woła te bez własnego strażnika. Pozostałe: SSRF w akcjach serwerowych i w przepisaniach, DoS, nieograniczony rozmiar ładunku. | Podbić do `16.2.11`. To zmiana łatki, nie wymaga migracji ani przepisywania kodu. **Nie robimy tego w audycie** (tryb „tylko raport") — do decyzji Igora osobno, wraz z pełnym przebiegiem `pnpm test && pnpm typecheck && pnpm build` przed wdrożeniem. | otwarte |
| SEC-A-04 | **niska** | `docs/security/owasp-top10-mapping.md:98` | Dokument twierdzi: „Dependency audit — `pnpm audit` w Fazie 28 Krok 1 — z 43 vulns ➜ 3 (overrides)". Stan faktyczny: **56 podatności — 30 wysokich, 22 średnie, 4 niskie**. | `pnpm audit` w tym worktree, po `pnpm install --frozen-lockfile`. | Trzeci wiersz tego dokumentu rozjechany z rzeczywistością (por. SEC-A-02, SEC-E-01). Liczba „3" sugeruje temat domknięty i zniechęca do ponownego sprawdzenia. | Zamiast liczby wpisać datę ostatniego przebiegu i sposób weryfikacji. Rozważyć `pnpm audit` w `.github/workflows/ci.yml` — dziś CI robi typecheck, lint i testy, ale nie sprawdza podatności. | otwarte |
| SEC-D-01 | **wysoka** | `lib/analytics/init-posthog-browser.ts:31-34` | Nagrywanie sesji PostHog maskuje pola formularzy (`maskAllInputs: true`), ale **nie maskuje tekstu wyświetlanego na stronie**. Selektor maskujący to `[data-ph-mask]` — a ten atrybut nie występuje w kodzie ANI RAZU poza samą linią konfiguracji. | `grep -rn "data-ph-mask" app components lib` → jedno trafienie, i jest nim deklaracja selektora w `init-posthog-browser.ts:33`. Zero elementów interfejsu z tym atrybutem. | Przy zgodzie na analitykę nagranie sesji odtwarza ekran panelu tak, jak go widział użytkownik: **nazwy kontrahentów, NIP-y, kwoty faktur, adresy**. Te dane trafiają do zewnętrznego przetwarzającego. Są to w dużej mierze dane KONTRAHENTÓW naszych klientów — ludzi, którzy nam niczego nie powierzali. Produkt, którego cała opowieść o zgodności opiera się na hostingu w UE i RODO, wynosi je do usługi analitycznej. | Dwie drogi. Węższa: dodać `data-ph-mask` do komponentów pokazujących dane kontrahenta i kwoty. Szersza i pewniejsza: odwrócić domyślne zachowanie — maskować wszystko (`maskTextSelector: "*"`) i odsłaniać wyjątki. Domyślne „wszystko jawne" wymaga pamiętania o każdym nowym komponencie i prędzej czy później zawiedzie. | otwarte |
| SEC-D-02 | **średnia** | `lib/analytics/init-posthog-browser.ts:28-38` | Zbieranie zdarzeń (`autocapture: true`) jest włączone także wtedy, gdy użytkownik **jeszcze nie odpowiedział** na baner zgody (stan `unset`). Wyłącza je dopiero jawna odmowa. | W `lib/analytics/consent.ts:19` stany to `granted` / `denied` / `unset`. W inicjalizacji: `opt_out_capturing_by_default: denied` oraz `if (!denied) posthog.opt_in_capturing()`. Dla `unset` obie ścieżki włączają zbieranie. Dla porównania nagrywanie sesji jest ustawione odwrotnie i poprawnie: `disable_session_recording: !granted`. | Przetwarzanie danych analitycznych przed uzyskaniem zgody. `autocapture` wysyła też treść klikniętego elementu, więc nie jest to wyłącznie kwestia formalna — z listy faktur może wyjechać nazwa kontrahenta. | Zrównać z nagrywaniem sesji: `autocapture: granted`. Zgoda domniemana z braku odpowiedzi nie jest zgodą. | otwarte |

---

## Świadomie odrzucone

Rzeczy, które wyglądały na znalezisko, a nie są. Zapisujemy, żeby nie
sprawdzać ich drugi raz za pół roku.

| Co | Dlaczego to NIE jest problem |
|---|---|
| `app/onboarding/import-source/page.tsx` i `magic-import/page.tsx` — `createAdminClient()` w parze z `getActiveOrgIdFromCookies()` | Obie strony przed sięgnięciem po dane sprawdzają członkostwo własnym zapytaniem: `from('memberships').eq('user_id', user.id).eq('organization_id', tenantId).eq('status','active')` i przekierowują, gdy wiersza nie ma. Identyfikator z ciasteczka jest więc zweryfikowany zanim cokolwiek wyjdzie z bazy. Komentarz w pliku wyjaśnia, czemu w ogóle omijają RLS: tuż po założeniu organizacji PostgREST zwracał `null` z powodu cache schematu i onboarding wpadał w pętlę przekierowań. |
| `app/actions/newsletter.ts` — akcja bez strażnika, omija RLS | Zapisuje wyłącznie adres e-mail na listę newslettera. Nie dotyka żadnych danych najemcy, jest limitowana po IP (5 na 10 minut). Oflagowana, bo leży w `app/actions/`, któremu narzędzie z definicji przypisuje „członek organizacji" — to błąd klasyfikacji narzędzia, nie kodu. |
| `app/api/portal/exports/generate/route.ts` — publiczny route z `createAdminClient()` | Autoryzacja tokenem: skrót tokenu → wiersz `accountant_access` → sprawdzenie `revoked_at`, `expires_at`, `access_level`, a na końcu jawne `if (tenantId !== accessRow.tenant_id) return 403`. Wzorzec poprawny. Osobno zgłoszone SEC-A-01 dotyczy tylko bloku `catch` w tym pliku. |
| `app/api/email/resend-webhook/route.ts` — webhook bez strażnika sesji | Weryfikuje podpis Svix (HMAC-SHA256) z `timingSafeEqual` i odrzuca żądanie, gdy `RESEND_WEBHOOK_SECRET` nie jest ustawiony. Dla webhooka podpis JEST uprawnieniem. **Do sprawdzenia osobno w dniu 3:** czy weryfikowana jest też tolerancja znacznika czasu — bez niej podpisane żądanie da się powtórzyć. |
| 8 stron w `app/admin/**` bez `requireAdmin()` w pliku | `app/admin/layout.tsx` woła `requireAdmin()`, a w App Routerze układ renderuje się przed stroną. **Zastrzeżenie:** dotyczy to wyłącznie stron. Akcje serwerowe i route handlery wchodzą z pominięciem układu — dla nich to rozumowanie nie działa i każda z nich musi mieć własne sprawdzenie. |
| Sekrety w kodzie i w historii gita | **Czysto.** Skaner przeszedł 1104 pliki śledzone i wszystkie 173 commity na 9 gałęziach. Zero prawdziwych trafień. Do repozytorium nigdy nie trafił żaden plik `.env` poza `.env.example` i `app/.env.example` — oba są wzorcami bez wartości. Cztery trafienia pierwszego przebiegu okazały się fałszywe: komunikat pomocy w `scripts/supabase-push-production.mjs:38` pokazujący format adresu z podstawieniami `[ref]` i `[HASŁO]`, oraz trzy fragmenty base64 osadzonego obrazka w `public/favicon/favicon.svg`, które złapał zbyt luźny wzorzec Turnstile. Oba wzorce w skrypcie poprawione. |
| Sekrety w pakiecie przeglądarki | **Czysto.** Zero zmiennych serwerowych w 157 plikach pobieranych przez przeglądarkę. Wynik jest wiarygodny, bo test przechodzi samokontrolę: w pakiecie znaleziono 6 zmiennych `NEXT_PUBLIC_*`, których obecność jest wymagana — gdyby ich nie było, „brak sekretów" oznaczałby zepsuty skan, nie bezpieczny kod. Trzy trafienia na zmiennych bez przedrostka to nie-sekrety, każde z ustalonym powodem: `AWS_REGION` trafiło we wbudowany w bibliotekę spis nazw regionów, `VAPID_SUBJECT` w sekcję kontaktową strony głównej (wartość publikowana celowo), `SENTRY_DSN` z założenia trafia do przeglądarki. |
| Mapy źródeł na produkcji | `public/sw.js.map` istnieje w buildzie, ale na produkcji zwraca **HTTP 307** — bramka auth przekierowuje na logowanie, bo rozszerzenie `.map` nie jest na liście wyjątków w `proxy.ts`. Sam `sw.js` odpowiada 200. Mapa nie jest publicznie dostępna. **Uwaga na przyszłość:** ta ochrona jest skutkiem ubocznym listy rozszerzeń, nie świadomą decyzją — dopisanie `.map` do wzorca w `proxy.ts` odsłoniłoby ją bez ostrzeżenia. |
| Granica `use server` | 25 plików z dyrektywą `use server`. Jedyny, który eksportuje coś innego niż funkcję zadeklarowaną na miejscu, to `app/(dashboard)/invoices/new/actions.ts` — plik zbiorczy re-eksportujący akcje z `components/invoices/*-actions.ts`. Pliki źródłowe same mają `use server`, a re-eksportowane typy są usuwane przy kompilacji. Zgodne z regułą. |
| Czyszczenie danych w Sentry | Obie konfiguracje mają `beforeSend` i obie robią coś sensownego. Serwer kasuje `event.request.cookies` i czyści nagłówki wrażliwe — z komentarzem wyjaśniającym, że `Authorization` potrafi trafić do zdarzenia nawet przy `sendDefaultPii: false`. Klient zamazuje token księgowej w adresie i w nazwie transakcji, co chroni ścieżkę `/accountant/[token]`. |
| `GHSA-6gpp-xcg3-4w24` — „Next.js: Middleware / Proxy bypass in App Router", wysoka waga, dotyczy naszej wersji | **Warunki nie są spełnione.** Doradztwo wymaga JEDNOCZEŚNIE Turbopacka i dokładnie jednego wpisu w `config.i18n.locales`. Produkcja buduje się `next build --webpack` (jawnie, patrz `package.json`), a klucza `i18n` w `next.config.ts` nie ma w ogóle. Odpada na obu warunkach. **Warunek dla przyszłych sesji:** gdyby ktoś kiedyś usunął `--webpack` z polecenia budowania albo dodał `config.i18n`, to ustalenie wraca jako krytyczne — cała bramka auth siedzi w `proxy.ts`. Zalecany przez Vercel obejściowy środek („autoryzuj w ścieżce danych strony, nie polegaj wyłącznie na warstwie pośredniczącej") jest sam w sobie dobrą zasadą i pokrywa się z tym, co sprawdzamy w dniu 2. |
