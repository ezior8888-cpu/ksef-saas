# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# Serwist — service worker z `next build --webpack` (nie commituj)
/public/sw.js
/public/sw.js.map

# production
/build

# misc
.DS_Store

# Cursor / Claude Code — lokalne worktree (nie commituj)
.claude/

# Lokalne notatki operatora (nie commituj)
.gemini.md
instrukcja.txt

# Certyfikaty KSeF (testowe i produkcyjne)
*.pem
*.key
*.crt
*.cer

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
# local env files
.env
.env.local
.env.*.local
!.env.example

.ksef-test-certs/

.npmrc

# Supabase CLI (cache po `supabase link` / update — nie commituj)
supabase/.temp/

# Local database dumps / backups — nie commituj
backup_*.sql

# Sentry Config File
.env.sentry-build-plugin

# Playwright E2E artifacts
/test-results/
/playwright-report/
/playwright/.cache/
/.auth/
docs/launch/
